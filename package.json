{
  "name": "qlik---answers---in-application",
  "version": "1.0.0",
  "description": "Generates natural-language insight text on the sheet using the **Qlik Cloud Assistants API** (`/api/v1/cloud-assistants`). No API key required.",
  "main": "answers-insights.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "docx": "^9.7.1"
  }
}
